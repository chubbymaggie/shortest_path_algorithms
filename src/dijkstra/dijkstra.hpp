#include "../undirected_graph/undirected_graph.hpp"

struct Dijkstra {
  undirected_graph<int, int> G;
  vertex<int> V;

  Dijkstra(undirected_graph<int, int> & G, vertex<int> & V) : G(G), V(V) { }

  

}; 
